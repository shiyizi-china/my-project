spring:
  datasource:
    url: jdbc:mysql://inline.proxy.rlwy.net:56928/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: SHiWjqZYIVQpQTicwhouYPEKAICQUJ
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 2
      connection-timeout: 3000
      idle-timeout: 60000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
sql:
  init:
    mode: never # 关闭自动执行SQL
cloud:
  alicloud:
    oss:
      endpoint: https://oss-cn-guangzhou.aliyuncs.com
      bucket-name: home-pageimage
      region: cn-guangzhou
mybatis:
  type-aliases-package: org.shiyizi.pojo
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
servlet:
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB
